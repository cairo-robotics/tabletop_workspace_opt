task:
  name: desk_pick_and_return_sa
  description: "Pick each object for the user, then return it to its original position"
  scene: scene_desk
  mode: pick_and_return
  pick_objects: [mug, stapler, pen_cup]
